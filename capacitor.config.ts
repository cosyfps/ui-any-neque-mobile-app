import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.duocuc.neque',
  appName: 'Ñeque',
  webDir: 'dist/neque/browser',
  server: {
    androidScheme: 'https',
  },
};

export default config;
