# NEQUE-7.1.2 — Mueve src/app/shared a la capa ui

## Estado

Terminado.

## Historia relacionada

`HU-7.1`

## Qué hace

Renombra `src/app/shared` a `src/app/ui/shared` y actualiza sus consumidores: los cuatro `@import` de `styles.scss`, el alias `@shared/*` de `tsconfig.json` y el mapper de Jest.

## Criterios de aceptación

- [x] Git registra los archivos como renombrados, no como borrado más alta.
- [x] Los 6 specs existentes siguen pasando sin cambios de contenido.
- [x] `build:prod` compila los estilos sin errores de ruta.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
