# NEQUE-7.1.3 — Mueve las páginas a la capa ui por feature

## Estado

Terminado.

## Historia relacionada

`HU-7.1`

## Qué hace

Renombra `pages/start` y `pages/forgot-password` a `ui/auth/`, y `pages/trainer` a `ui/trainer/`. Reescribe los `loadComponent` de `app.routes.ts`.

## Criterios de aceptación

- [x] No queda ninguna carpeta `src/app/pages`.
- [x] Cada spec viaja junto al archivo que prueba.
- [x] Todas las rutas siguen resolviendo a su componente.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
