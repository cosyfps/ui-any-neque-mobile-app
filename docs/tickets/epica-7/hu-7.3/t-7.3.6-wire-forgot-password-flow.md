# NEQUE-7.3.6 — Conecta la recuperación de contraseña al puerto

## Estado

Terminado.

## Historia relacionada

`HU-7.3`

## Qué hace

Reemplaza los tres `setTimeout` simulados de `ForgotPasswordPage` por llamadas a `AUTH_PORT`, con estado de error propio.

## Criterios de aceptación

- [x] El envío de código llama al puerto con el correo ingresado y solo avanza de paso si responde bien.
- [x] La verificación envía el código concatenado y adopta la sesión resultante.
- [x] Un código inválido muestra el error y no navega.
- [x] El countdown de reenvío sigue funcionando y se limpia al destruir la página.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
