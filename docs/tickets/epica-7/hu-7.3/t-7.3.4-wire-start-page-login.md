# NEQUE-7.3.4 — Conecta el login al puerto de autenticación

## Estado

Terminado.

## Historia relacionada

`HU-7.3`

## Qué hace

Reemplaza el `// TODO: wire to auth service` de `StartPage.onLogin()` por una llamada a `SessionFacade.login()`, con manejo del error de credenciales y redirección según rol.

## Criterios de aceptación

- [x] Un login válido navega al home del rol devuelto por el backend.
- [x] Credenciales inválidas muestran el mensaje en `.nq-field-error` y no navegan.
- [x] El error se limpia al editar cualquiera de los dos campos.
- [x] Un segundo submit mientras hay uno en curso no llama al puerto.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
