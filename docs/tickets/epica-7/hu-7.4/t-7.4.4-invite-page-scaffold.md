# NEQUE-7.4.4 — Agrega la pantalla pública de invitación

## Estado

Terminado.

## Historia relacionada

`HU-7.4`

## Qué hace

Crea la ruta `/invite/:token` y la `InvitationFacade`, con los estados de carga, error y éxito resueltos por `<nq-page-state>`.

## Criterios de aceptación

- [x] La ruta es pública: no exige sesión.
- [x] Un token inválido muestra el motivo concreto y ofrece ir al login, sin botón de reintentar.
- [x] La pantalla saluda al alumno por su nombre y menciona quién lo invitó.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
