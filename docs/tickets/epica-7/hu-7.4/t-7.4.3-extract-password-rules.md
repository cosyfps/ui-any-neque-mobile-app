# NEQUE-7.4.3 — Extrae las reglas de contraseña a un módulo compartido

## Estado

Terminado.

## Historia relacionada

`HU-7.4`

## Qué hace

Saca las cuatro reglas de contraseña de `StartPage` a `ui/shared/validators/password-rules.ts`, y hace que login e invitación las consuman.

## Por qué

Las dos pantallas pedían las mismas reglas: duplicarlas garantizaba que se desincronizaran.

## Criterios de aceptación

- [x] Login e invitación evalúan exactamente las mismas cuatro reglas.
- [x] Las reglas son funciones puras probadas por separado.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
