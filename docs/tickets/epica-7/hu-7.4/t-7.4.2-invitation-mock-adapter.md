# NEQUE-7.4.2 — Implementa el adapter mock de invitaciones

## Estado

Terminado.

## Historia relacionada

`HU-7.4`

## Qué hace

Crea `InvitationMockAdapter` con tres tokens semilla que cubren los tres caminos de la pantalla.

## Criterios de aceptación

- [x] `inv-valida`, `inv-expirada` e `inv-usada` permiten probar los tres casos a mano.
- [x] Cada caso inválido devuelve un mensaje distinto y accionable.
- [x] Aceptar una invitación la marca como usada: no se puede aceptar dos veces.
- [x] Una contraseña que no cumple el mínimo es rechazada por el adapter.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
