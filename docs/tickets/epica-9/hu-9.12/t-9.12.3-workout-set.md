# NEQUE-9.12.3 — Agrega el registro de series reales

## Estado

Terminado.

## Historia relacionada

`HU-9.12`

## Qué hace

Crea `WorkoutSet` en `domain/workouts`, agrega `WorkoutExerciseLog.sets` y cambia `logSet` para recibir la serie completa.

## Por qué

Sin este registro la progresión de carga se pierde y el entrenador no puede ajustar.

## Criterios de aceptación

- [x] Reintentar la misma serie reemplaza en vez de acumular.
- [x] La serie se fecha con el reloj inyectado.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
