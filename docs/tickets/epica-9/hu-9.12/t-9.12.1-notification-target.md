# NEQUE-9.12.1 — Cambia la ruta guardada por tipo e id

## Estado

Terminado.

## Historia relacionada

`HU-9.12`

## Qué hace

Reemplaza `AppNotification.actionRoute` por `targetType` + `targetId`, con el enum `NotificationTarget`.

## Por qué

La base no conoce la navegación del front: guardar una ruta la ata a la app y rompe cualquier cambio de rutas.

## Criterios de aceptación

- [x] El modelo, el adapter, la semilla y los specs quedan alineados.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
