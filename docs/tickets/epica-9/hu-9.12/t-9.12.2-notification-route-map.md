# NEQUE-9.12.2 — Agrega el mapa de destino a ruta

## Estado

Terminado.

## Historia relacionada

`HU-9.12`

## Qué hace

Crea `notificationRoute()` en `ui/shared/navigation` con su spec.

## Por qué

El mapeo vive en un solo archivo: si mañana cambia la ruta de una entidad, cambia ahí y en ningún otro sitio.

## Criterios de aceptación

- [x] La página de notificaciones deja de navegar a una cadena que venía de datos.
- [x] Una notificación informativa no abre nada.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
