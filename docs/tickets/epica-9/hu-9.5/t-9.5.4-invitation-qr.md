# NEQUE-9.5.4 — Agrega el código QR del enlace

## Estado

Terminado.

## Historia relacionada

`HU-9.5`

## Qué hace

Agrega el QR con `import()` dinámico de la librería.

## Por qué

La librería pesa más que la pantalla y solo la necesita quien toca «Ver código QR»; estática viajaría en el chunk de `/trainer`.

## Criterios de aceptación

- [x] El QR queda en su propio chunk lazy.
- [x] Reemitir descarta el código anterior.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
