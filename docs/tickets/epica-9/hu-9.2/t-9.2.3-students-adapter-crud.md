# NEQUE-9.2.3 — Implementa alta, edición y estado en el adapter de alumnos

## Estado

Terminado.

## Historia relacionada

`HU-9.2`

## Qué hace

Reescribe `StudentsMockAdapter` con `create`, `edit` y `setStatus`, inyectando `CLOCK`.

## Por qué

El alta tiene que rechazar correos repetidos igual que lo hará el backend, o la pantalla nunca ejercita ese camino.

## Criterios de aceptación

- [x] Un correo duplicado devuelve `conflict`.
- [x] El alta se fecha con el reloj inyectado.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
