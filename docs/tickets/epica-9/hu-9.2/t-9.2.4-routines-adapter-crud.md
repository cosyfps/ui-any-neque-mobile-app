# NEQUE-9.2.4 — Implementa la construcción y asignación de rutinas

## Estado

Terminado.

## Historia relacionada

`HU-9.2`

## Qué hace

Reescribe `RoutinesMockAdapter` con `create`, `update`, `assign` y `archive`, y asigna ids a días y ejercicios.

## Por qué

El constructor manda días y ejercicios sin id; asignarlos es trabajo del backend y el mock tiene que imitarlo.

## Criterios de aceptación

- [x] `assign` activa la rutina y archiva la anterior del alumno en una operación.
- [x] Una rutina nace sin asignar.
- [x] `listForTrainer` del catálogo filtra públicos más propios.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
