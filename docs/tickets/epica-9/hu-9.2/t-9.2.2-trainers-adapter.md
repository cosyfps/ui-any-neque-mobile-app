# NEQUE-9.2.2 — Agrega el adapter de entrenadores

## Estado

Terminado.

## Historia relacionada

`HU-9.2`

## Qué hace

Crea `TrainersMockAdapter` con la semilla de Kelvin y su perfil profesional.

## Por qué

El entrenador no existía como modelo: hasta ahora solo se le conocía por `Student.trainerId`.

## Criterios de aceptación

- [x] `getById` falla con `not_found` ante un id desconocido.
- [x] No entrega referencias a su estado interno.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
