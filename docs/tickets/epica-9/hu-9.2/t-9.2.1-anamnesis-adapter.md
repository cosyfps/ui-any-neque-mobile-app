# NEQUE-9.2.1 — Agrega el adapter de anamnesis con su semilla

## Estado

Terminado.

## Historia relacionada

`HU-9.2`

## Qué hace

Crea `AnamnesisMockAdapter` y la semilla con la anamnesis de Ana.

## Por qué

Sin un alumno que ya la tenga registrada no se puede probar el camino de edición, solo el de alta.

## Criterios de aceptación

- [x] `save` crea o edita conservando el id.
- [x] Fecha la edición con el reloj inyectado, no con `new Date()`.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
