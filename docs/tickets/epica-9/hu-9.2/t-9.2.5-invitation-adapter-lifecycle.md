# NEQUE-9.2.5 — Implementa el ciclo de vida de la invitación

## Estado

Terminado.

## Historia relacionada

`HU-9.2`

## Qué hace

Implementa `getForStudent`, `create` y `revoke` en `InvitationMockAdapter`, resolviendo el alumno por `STUDENTS_PORT`.

## Por qué

Leer la semilla de alumnos directamente dejaba sin invitación a todo alumno creado en la sesión: el alta y la invitación tienen que ver la misma base, como la verá el BFF.

## Criterios de aceptación

- [x] Un alumno dado de alta en la sesión puede recibir invitación.
- [x] Emitir una revoca la pendiente anterior.
- [x] La vigencia sale de `INVITATION_TTL_HOURS`.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
