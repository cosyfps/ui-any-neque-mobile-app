# NEQUE-9.8.1 — Agrega la biblioteca de rutinas

## Estado

Terminado.

## Historia relacionada

`HU-9.8`

## Qué hace

Crea `TrainerRoutinesFacade` y `TrainerRoutinesPage` con las pestañas Asignadas y Sin asignar.

## Por qué

El nombre del alumno se resuelve en la facade porque el BFF devolverá la rutina con ese JOIN ya hecho.

## Criterios de aceptación

- [x] Cada rutina muestra su alumno, sus días y su objetivo.
- [x] Una rutina cuyo alumno ya no está en la cartera se muestra como desconocido.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
