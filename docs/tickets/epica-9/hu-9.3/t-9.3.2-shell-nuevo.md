# NEQUE-9.3.2 — Agrega el shell del entrenador con sus cuatro pestañas

## Estado

Terminado.

## Historia relacionada

`HU-9.3`

## Qué hace

Crea `TrainerLayoutPage` sobre el mixin `nq-shell`, con Inicio, Alumnos, Rutinas y Perfil, `aria-label` en cada pestaña y la pestaña recordada.

## Por qué

La ficha de un alumno y el constructor viven dentro del shell sin ser pestañas; sin recordar la anterior las cuatro quedan apagadas.

## Criterios de aceptación

- [x] La pestaña activa se deriva de `NavigationEnd` ignorando los query params.
- [x] Una ruta hija mantiene encendida la pestaña que la abrió.
- [x] Cada pestaña tiene nombre accesible.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
