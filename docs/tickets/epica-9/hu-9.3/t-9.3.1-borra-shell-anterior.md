# NEQUE-9.3.1 — Borra el shell y el dashboard anteriores

## Estado

Terminado.

## Historia relacionada

`HU-9.3`

## Qué hace

Elimina `trainer-layout.page.*` y `dashboard.page.*` con sus specs, y las cuatro rutas alias.

## Por qué

El esqueleto anterior apuntaba las cuatro pestañas al mismo componente; mantenerlo mientras se construye el nuevo solo duplica rutas muertas.

## Criterios de aceptación

- [x] No queda ninguna referencia a `DashboardPage`.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
