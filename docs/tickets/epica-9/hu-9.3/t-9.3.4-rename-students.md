# NEQUE-9.3.4 — Renombra `clients` a `students`

## Estado

Terminado.

## Historia relacionada

`HU-9.3`

## Qué hace

Cambia `/trainer/clients` por `/trainer/students` en rutas, carpetas y selectores, y actualiza `homeRouteForRole` a `/trainer/home`.

## Por qué

En la interfaz siempre se dijo «alumno»; el nombre técnico heredado ya no tenía a quién servir.

## Criterios de aceptación

- [x] Ninguna ruta antigua queda referenciada.
- [x] El login del entrenador entra en `/trainer/home`.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
