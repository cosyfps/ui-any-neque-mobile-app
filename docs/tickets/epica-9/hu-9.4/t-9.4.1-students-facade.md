# NEQUE-9.4.1 — Agrega la facade de la cartera

## Estado

Terminado.

## Historia relacionada

`HU-9.4`

## Qué hace

Crea `TrainerStudentsFacade` con `AsyncState<Student[]>` y los computed de búsqueda y filtro.

## Por qué

La búsqueda es un computed sobre la lista cargada, no una consulta nueva: la cartera cabe en memoria y así escribir no dispara un `loading` por tecla.

## Criterios de aceptación

- [x] `viewState()` distingue el vacío por búsqueda del de cartera vacía.
- [x] Solo consulta con un entrenador en sesión.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
