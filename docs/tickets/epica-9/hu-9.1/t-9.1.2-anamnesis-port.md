# NEQUE-9.1.2 — Agrega el puerto de anamnesis

## Estado

Terminado.

## Historia relacionada

`HU-9.1`

## Qué hace

Crea `ANAMNESIS_PORT` con `getByStudent` y `save`.

## Por qué

La anamnesis es única por alumno y se edita: separar crear de actualizar habría obligado a la UI a saber cuál de las dos llamar.

## Criterios de aceptación

- [x] `getByStudent` devuelve `null` cuando todavía no está registrada.
- [x] `save` sirve para el alta y para la edición.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
