# NEQUE-9.1.3 — Agrega el dueño al ejercicio del catálogo

## Estado

Terminado.

## Historia relacionada

`HU-9.1`

## Qué hace

Agrega `Exercise.ownerTrainerId` con `isPublicExercise()` e `isVisibleToTrainer()`, y actualiza la semilla y sus specs.

## Por qué

Los ejercicios propios son privados: sin el dueño en el modelo no hay forma de filtrarlos y un entrenador vería los de otro.

## Criterios de aceptación

- [x] `null` significa catálogo público.
- [x] Un entrenador solo ve los públicos y los suyos.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
