# NEQUE-9.1.1 — Agrega el modelo de anamnesis

## Estado

Terminado.

## Historia relacionada

`HU-9.1`

## Qué hace

Crea `Anamnesis`, `AnamnesisInput`, `EmergencyContact` y las funciones puras `isAnamnesisUsable()` y `hasClinicalFlags()` en `domain/students/model`.

## Por qué

La ficha del alumno y el constructor de rutinas tienen que coincidir en qué consideran «completa»; con la regla duplicada en dos pantallas terminan discrepando.

## Criterios de aceptación

- [x] Objetivo declarado y disponibilidad semanal son obligatorios; el resto es opcional.
- [x] `hasClinicalFlags()` ignora los campos de solo espacios.
- [x] El contacto de urgencia guarda nombre, teléfono y parentesco por separado.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
