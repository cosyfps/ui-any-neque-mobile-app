# NEQUE-9.1.4 — Extiende el puerto de alumnos con alta, edición y estado

## Estado

Terminado.

## Historia relacionada

`HU-9.1`

## Qué hace

Agrega `create`, `edit` y `setStatus` a `STUDENTS_PORT`, con `StudentInput` y `StudentEditableFields`.

## Por qué

El entrenador registra alumnos y suspende cuentas; el puerto solo sabía leer y dejar que el propio alumno editara su perfil.

## Criterios de aceptación

- [x] `create` falla con `conflict` si el correo ya existe.
- [x] `StudentEditableFields` excluye el correo: cambiarlo rompería las invitaciones emitidas.
- [x] El alumno nunca se elimina, se suspende y se reactiva.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
