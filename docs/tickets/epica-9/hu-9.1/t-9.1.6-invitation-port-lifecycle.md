# NEQUE-9.1.6 — Agrega el ciclo de vida de la invitación

## Estado

Terminado.

## Historia relacionada

`HU-9.1`

## Qué hace

Agrega `getForStudent`, `create` y `revoke` a `INVITATION_PORT`, el estado `revoked` y la constante `INVITATION_TTL_HOURS = 48`.

## Por qué

La constante vive en dominio para que el contador que ve el entrenador y el vencimiento que aplique el backend salgan del mismo número.

## Criterios de aceptación

- [x] `create` invalida la invitación pendiente anterior del mismo alumno.
- [x] `revoke` deja al alumno sin invitación activa sin emitir otra.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
