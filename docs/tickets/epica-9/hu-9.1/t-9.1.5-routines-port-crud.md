# NEQUE-9.1.5 — Extiende los puertos de rutinas y catálogo

## Estado

Terminado.

## Historia relacionada

`HU-9.1`

## Qué hace

Agrega `create`, `update`, `assign` y `archive` a `ROUTINES_PORT`, y `create` más `listForTrainer` a `EXERCISE_CATALOG_PORT`.

## Por qué

`assign` activa y archiva la anterior en una sola operación: en dos pasos queda un hueco donde el alumno tiene dos rutinas activas o ninguna.

## Criterios de aceptación

- [x] `assign` es una sola llamada.
- [x] `listForTrainer` devuelve el catálogo público más los propios del entrenador.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
