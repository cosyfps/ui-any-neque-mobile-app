# NEQUE-11.1.1 — Tokens de color accesibles

## Estado

Terminado.

## Historia relacionada

`HU-11.1`

## Hallazgo de auditoría

[`T-3`](../../../auditoria/2026-09-18-auditoria-app-alumno.md) — ver el informe para el detalle y la severidad.

## Qué hace

Agrega `--nq-primary-strong` (#0f766e) y `--nq-gradient-strong-*`, y corrige `--nq-text-muted` a #647976, `--nq-danger` a #c62828 y `--nq-warning` a #b45309.

## Por qué

Los CTA principales tenían texto blanco a 1.45:1 sobre el degradado y a 2.55:1 sobre el primario. Ninguna pantalla pasaba WCAG AA.

## Criterios de aceptación

- [x] `--nq-primary-strong` da 5.50:1 contra blanco en ambos sentidos.
- [x] `--nq-text-muted` sube de 2.50:1 a 4.66:1.
- [x] `--nq-danger` sube de 3.76:1 a 5.58:1 y `--nq-warning` de 2.15:1 a 5.05:1.
- [x] `--nq-primary`, `--nq-primary-light`, `--nq-primary-dark` y el degradado de marca no cambian.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido o actualizado cuando el ticket toca código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Verificado en navegador a 375×812 y 320×568 cuando toca UI.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
