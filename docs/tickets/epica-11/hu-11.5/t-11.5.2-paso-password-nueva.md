# NEQUE-11.5.2 — Paso de contraseña nueva

## Estado

Terminado.

## Historia relacionada

`HU-11.5`

## Hallazgo de auditoría

[`F-1`](../../../auditoria/2026-09-18-auditoria-app-alumno.md) — ver el informe para el detalle y la severidad.

## Qué hace

Agrega el tercer paso a `/forgot-password` reutilizando `passwordRules()` y el layout de campos de `/invite`: contraseña, confirmación, reglas en grilla y auto-login al guardar.

## Por qué

La pantalla prometía restablecer la contraseña y nunca la pedía.

## Criterios de aceptación

- [x] El flujo es email → código → contraseña nueva, con la misma animación de pasos.
- [x] El botón se habilita solo con las cuatro reglas cumplidas y la confirmación coincidiendo.
- [x] Un segundo envío mientras guarda no dispara una segunda llamada.
- [x] Al guardar, la sesión se adopta y redirige al home del rol.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido o actualizado cuando el ticket toca código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Verificado en navegador a 375×812 y 320×568 cuando toca UI.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
