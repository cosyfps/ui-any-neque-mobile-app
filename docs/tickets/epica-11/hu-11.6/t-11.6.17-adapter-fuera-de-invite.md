# NEQUE-11.6.17 — Adapter fuera de la página de invitación

## Estado

Terminado.

## Historia relacionada

`HU-11.6`

## Hallazgo de auditoría

[`I-1`](../../../auditoria/2026-09-18-auditoria-app-alumno.md) — ver el informe para el detalle y la severidad.

## Qué hace

Mueve `InvitationMockAdapter` a un `provideInvitationData()` y lo registra en la ruta.

## Por qué

Es la única página que importa de `infrastructure`: conectar el BFF obligaría a editar un archivo de `ui`, rompiendo el criterio de éxito de la arquitectura.

## Criterios de aceptación

- [x] `InvitationMockAdapter` se registra con `provideInvitationMockAdapters()` en la ruta.
- [x] `invite.page.ts` ya no importa de `infrastructure`.
- [x] Conectar el BFF no obliga a tocar ningun archivo de `ui`.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido o actualizado cuando el ticket toca código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Verificado en navegador a 375×812 y 320×568 cuando toca UI.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
