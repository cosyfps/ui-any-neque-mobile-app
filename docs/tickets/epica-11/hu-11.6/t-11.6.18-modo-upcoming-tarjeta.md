# NEQUE-11.6.18 — Modo upcoming de la tarjeta

## Estado

Terminado.

## Historia relacionada

`HU-11.6`

## Hallazgo de auditoría

[`H-4`](../../../auditoria/2026-09-18-auditoria-app-alumno.md) — ver el informe para el detalle y la severidad.

## Qué hace

Usa el modo `upcoming` de `<nq-workout-card>` o elimínalo.

## Por qué

La mitad del componente, su `@Output() toggled` y todo el CSS del switch son código muerto.

## Nota de cierre

Se elimino en vez de implementarse: no lo usaba ninguna pantalla.

## Criterios de aceptación

- [x] La tarjeta pierde el modo `upcoming`, su `@Output() toggled` y el CSS del switch.
- [x] Las paginas que la usan dejan de pasar `mode`.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido o actualizado cuando el ticket toca código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build` en verde.
- [x] Sin warnings nuevos de budget en `build`.
- [x] Verificado en navegador a 375×812 y 320×568 cuando toca UI.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
