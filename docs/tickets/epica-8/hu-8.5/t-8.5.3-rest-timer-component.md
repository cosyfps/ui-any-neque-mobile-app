# NEQUE-8.5.3 — Agrega el anillo de progreso del descanso

## Estado

Terminado.

## Historia relacionada

`HU-8.5`

## Qué hace

Crea `<nq-ring-progress>` en SVG, con la matemática del anillo en `chart-math.ts`.

## Criterios de aceptación

- [x] El anillo empieza arriba y se vacía conforme corre el descanso.
- [x] El progreso se recorta al rango 0-1.
- [x] El componente expone `role="img"` con los segundos restantes en texto.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
