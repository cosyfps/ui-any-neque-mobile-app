# NEQUE-8.3.3 — Agrega el componente compartido de tarjeta de entrenamiento

## Estado

Terminado.

## Historia relacionada

`HU-8.3`

## Qué hace

Crea `<nq-workout-card>` con los modos `progress` (barra y chevron) y `upcoming` (toggle), más el estado activo o atenuado.

## Criterios de aceptación

- [x] El avance se recorta al rango 0-100 y se redondea a entero.
- [x] El avatar usa las iniciales de las dos primeras palabras del título.
- [x] El toggle expone `role="switch"` con `aria-checked`.
- [x] El tipo del modo es exacto, no `string`.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
