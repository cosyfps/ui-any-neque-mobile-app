# NEQUE-8.4.4 — Agrega el cierre de sesión con modal de celebración

## Estado

Terminado.

## Historia relacionada

`HU-8.4`

## Qué hace

Agrega el botón de marcar la sesión como completada y el modal breve de celebración.

## Por qué

El overlay vive siempre en el DOM y conmuta la clase `open`: `.nq-overlay` arranca en opacity 0 y sin eventos, así que con `@if` sin esa clase el modal queda invisible.

## Criterios de aceptación

- [x] Cerrar la sesión deja el día al 100% y reemplaza el botón por el aviso de completada.
- [x] La celebración es un check y un texto, sin confeti ni animaciones excesivas.
- [x] El modal se cierra tocando fuera o el botón.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
