# NEQUE-8.4.1 — Agrega la facade de rutina del alumno

## Estado

Terminado.

## Historia relacionada

`HU-8.4`

## Qué hace

Crea `StudentRoutineFacade` con la rutina activa, el día seleccionado, su sesión asociada y el avance.

## Criterios de aceptación

- [x] Al cargar selecciona el día de hoy según el reloj inyectado.
- [x] Un alumno sin rutina deja la pantalla en estado vacío, no en carga eterna.
- [x] Un día sin entrenamiento deja `selectedDay` en null.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
