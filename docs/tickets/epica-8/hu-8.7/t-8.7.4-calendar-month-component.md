# NEQUE-8.7.4 — Agrega el calendario mensual en CSS grid

## Estado

Terminado.

## Historia relacionada

`HU-8.7`

## Qué hace

Renderiza la grilla del mes con puntos en los días que tienen citas, sin SVG ni librerías.

## Criterios de aceptación

- [x] Los días de otro mes se distinguen visualmente.
- [x] Un día con citas muestra un punto; las canceladas no cuentan.
- [x] Cada día expone una etiqueta accesible que avisa si tiene sesiones.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
