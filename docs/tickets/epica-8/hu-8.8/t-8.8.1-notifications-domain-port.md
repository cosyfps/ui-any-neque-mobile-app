# NEQUE-8.8.1 — Define el dominio de notificaciones

## Estado

Terminado.

## Historia relacionada

`HU-8.8`

## Qué hace

Crea `AppNotification` con su tipo y el puerto `NOTIFICATIONS_PORT`.

## Por qué

El puerto no expone un contador de no leídas: se deriva de la lista que ya trae `listByUser`, así que es un computed de la facade.

## Criterios de aceptación

- [x] Distingue rutina, sesión, mensaje y sistema.
- [x] Una notificación sin leer se identifica por no tener fecha de lectura.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
