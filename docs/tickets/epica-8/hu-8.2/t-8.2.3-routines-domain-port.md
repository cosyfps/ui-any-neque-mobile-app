# NEQUE-8.2.3 — Define el dominio de rutinas y ejercicios

## Estado

Terminado.

## Historia relacionada

`HU-8.2`

## Qué hace

Crea `Exercise`, `RoutineExercise`, `RoutineDay` y `Routine`, con `ROUTINES_PORT` y `EXERCISE_CATALOG_PORT`.

## Criterios de aceptación

- [x] Una rutina agrupa días y cada día ordena sus ejercicios con su prescripción completa.
- [x] `dayForWeekday()` devuelve null en un día sin entrenamiento, no lanza.
- [x] El catálogo de ejercicios es independiente de cualquier rutina.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
