# NEQUE-8.2.1 — Define el dominio de alumnos y evaluaciones

## Estado

Terminado.

## Historia relacionada

`HU-8.2`

## Qué hace

Crea `Student`, `Assessment`, `BodyMeasurements` y `calculateBmi()`, con los puertos `STUDENTS_PORT` y `ASSESSMENTS_PORT`.

## Por qué

Los puertos declaran ya `listByTrainer` y `create`, que solo usará el entrenador: la Épica 9 extiende adapters en vez de rehacer puertos.

## Criterios de aceptación

- [x] `calculateBmi()` aplica los cortes de la OMS y devuelve null ante datos inválidos.
- [x] `BodyMeasurements` usa propiedades explícitas, no un índice dinámico.
- [x] El puerto de evaluaciones permite pedir la última sin traer todo el historial.

## Definición de terminado

- [x] Criterios de aceptación cumplidos.
- [x] `.spec.ts` incluido cuando el ticket agrega código.
- [x] `lint`, `typecheck`, `format:check`, `test:coverage` y `build:prod` en verde.
- [x] Sin warnings nuevos de budget en `build:prod`.
- [x] Tablero de `docs/BACKLOG.md` actualizado.
